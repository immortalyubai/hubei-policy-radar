# Public URL of the deployed Policy Radar site.
POLICY_RADAR_URL=https://your-policy-radar.example.com

# Private write key for /api/ingest and /api/import/wechat.
# Generate a long random value. Never commit the real key.
POLICY_INGEST_KEY=replace-with-a-long-random-value
